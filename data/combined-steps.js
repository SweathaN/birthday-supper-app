// Note: Sweet C6 kozhakattai now uses rice flour + tapioca (translucent modak style),
// while savory C2 uses wheat starch + tapioca (crystal dim sum style).
// The two doughs can no longer share a batch — each is kneaded separately.
window.PB_COMBINED_STEPS = [];
